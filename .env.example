# คัดลอกเป็น .env แล้วเติมค่าจริง — ห้าม commit ไฟล์ .env ลง git
BMS_ADDR=:8080
BMS_DATABASE_URL=postgres://bms:CHANGE_ME@localhost:5432/bms?sslmode=disable
# สร้างด้วย: openssl rand -base64 48
BMS_JWT_SECRET=
